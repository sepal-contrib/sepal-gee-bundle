"""Shared pure-logic primitives for bundle apps.

Per bundle CLAUDE.md: this package may contain pure constants and pure
functions only. No state, no I/O, no UI components, and no imports from
any `apps/<app_name>/` package.
"""
