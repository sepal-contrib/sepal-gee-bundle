services:
  sepal-gee-bundle:
    ports:
      - "8768:8768"
    extra_hosts:
      - "${SEPAL_HOST:?SEPAL_HOST must be set}:host-gateway"
    environment:
      SEPAL_HOST: ${SEPAL_HOST}
      EECLIENT_INSECURE_TLS: "1"  # local dev: Python doesn't trust the Caddy local CA
